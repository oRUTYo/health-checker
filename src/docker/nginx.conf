server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Proxy API to backend service
    location ~ ^/(ping|predict) {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # SPA fallback
    location / {
        try_files $uri /index.html;
    }
}
